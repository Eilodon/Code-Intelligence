# Rust build output
/target/

# Index database (per-project, generated)
.codeindex/
*.db
*.db-wal
*.db-shm

# Coverage output — but keep the committed parity-test fixture
.coverage
coverage.out
coverage.xml
lcov.info
coverage/
.nyc_output/
!crates/ci-core/tests/fixtures/synthetic_project/lcov.info

# Claude Code personal overrides (not shared with team)
.claude/settings.local.json

# Benchmark harness venv + raw run output
benchmarks/.venv/
benchmarks/**/results.json
__pycache__/

# OS / editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/mcp.json
.idea/
*.swp
*.swo
