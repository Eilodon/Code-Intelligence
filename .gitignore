# Rust build output
/target/
crates/calm-core/tests/fixtures/rust_workspace/target/

# Index database (per-project, generated)
.calm/
*.db
*.db-wal
*.db-shm

# scip-typescript's --infer-tsconfig side effect when the P3.2 ignored test
# runs against the (deliberately tsconfig-less) js/ fixture directly.
crates/calm-core/tests/fixtures/multi_lang_workspace/js/tsconfig.json

# Coverage output — but keep the committed parity-test fixture
.coverage
coverage.out
coverage.xml
lcov.info
coverage/
.nyc_output/
!crates/calm-core/tests/fixtures/synthetic_project/lcov.info

# Claude Code personal overrides (not shared with team)
.claude/settings.local.json

# Benchmark harness venv + raw run output
benchmarks/.venv/
benchmarks/**/results.json
benchmarks/scratch/*.json
__pycache__/

# resolution benchmark's cloned OSS corpora — external repos, not ours to commit
benchmarks/resolution/corpus/

# OS / editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/mcp.json
.idea/
*.swp
*.swo

# npm platform-package binaries — populated by npm/stage-release.sh from a
# tagged GitHub Release, not committed (same ~95MB prebuilt binaries
# .github/workflows/release.yml already publishes, just re-packaged)
npm/*/calm
npm/node_modules/
npm/*/node_modules/
